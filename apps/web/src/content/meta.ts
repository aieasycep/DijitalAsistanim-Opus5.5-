/**
 * Content versions (sitemap `lastModified`, legal headers). Bump `LEGAL_VERSION` and
 * `LEGAL_EFFECTIVE_DATE` with every material change to `/privacy` or `/terms`; material changes are
 * announced in the app at least 15 days before they take effect (Part 5 §3.2 `#degisiklikler`).
 */
export const LEGAL_VERSION = '1.0';
export const LEGAL_EFFECTIVE_DATE = '2026-09-24';

/** Last content change of the marketing and support pages. */
export const SITE_CONTENT_DATE = '2026-09-24';
