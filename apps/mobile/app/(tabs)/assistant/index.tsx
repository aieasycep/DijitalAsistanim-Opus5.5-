// M-ASST-01 tab root: shell header and bootstrap-driven state until the Assistant home (T-8.15) screen.
import { TabRoot } from '../../../src/features/shell/TabRoot';

export default function AssistantTab() {
  return <TabRoot tab="assistant" />;
}
