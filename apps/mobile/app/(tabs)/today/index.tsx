// M-TD-01 tab root: shell header and bootstrap-driven state until the Today (T-8.08) screen.
import { TabRoot } from '../../../src/features/shell/TabRoot';

export default function TodayTab() {
  return <TabRoot tab="today" />;
}
