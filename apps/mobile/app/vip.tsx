// M-VIP-01 VIP list (T-8.16); the onboarding VIP step shares this URL behind its own guard.
import { VipScreen } from '../src/features/person/VipScreen';

export default function VipRoute() {
  return <VipScreen />;
}
