// M-ASST-02 assistant thread (T-8.15); `new` is an unsaved thread.
import { ChatScreen } from '../../src/features/assistant/ChatScreen';

export default function ChatRoute() {
  return <ChatScreen />;
}
