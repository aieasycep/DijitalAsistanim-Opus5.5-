// M-GL-12: blocking update screen (full-screen modal, no back gesture; see the root layout).
import { UpdateRequiredScreen } from '../src/features/shell/UpdateRequiredScreen';

export default UpdateRequiredScreen;
