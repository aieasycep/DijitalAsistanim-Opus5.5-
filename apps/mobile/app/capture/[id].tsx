// M-CAP-05…07 analysis, results and success of one capture (T-8.17).
import { CaptureDetailScreen } from '../../src/features/capture/CaptureDetailScreen';

export default function CaptureDetailRoute() {
  return <CaptureDetailScreen />;
}
