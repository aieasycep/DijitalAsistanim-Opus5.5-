# Continuous native generation: `expo prebuild` output is never committed. Anchored to the app
# root: the local native modules (`modules/*/ios`, `modules/*/android`) are source.
/ios/
/android/
.expo/
.expo-export/
expo-env.d.ts
