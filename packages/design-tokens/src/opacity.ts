/** Opacity levels observed in the design (disabled controls, rejected cards, offline content). */
export const opacity = {
  disabled: 0.4,
  rejected: 0.45,
  inactiveRule: 0.5,
  inactiveChapter: 0.55,
  offlineContent: 0.75,
} as const;
